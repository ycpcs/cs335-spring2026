#!/bin/env python3
import socket

udp = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
#udp.bind(("0.0.0.0", 9013))

data = b"Get me the time\n"
udp.sendto(data, ("10.9.0.5", 9013))

data, (ip, port) = udp.recvfrom(1024)
print ("Source {}:{}: {}".format(ip, port, str(data, 'utf-8')))